# --- Application settings ---
DB_PATH=data/doppler.db

# Session secret for cookie encryption/signing (REQUIRED)
# Generate with: openssl rand -base64 32
DOPPLER_SESSION_SECRET=CHANGE_ME_TO_RANDOM_SECRET_FOR_PRODUCTION

# --- Garage S3 setup (for docker compose only) ---
# The garage-init service needs this to configure Garage
GARAGE_RPC_SECRET=8e582479c52ad04c8a89d58e3af525f63334b74adf3722fe8fddcbba861c67d4

# --- S3 settings (for photo upload feature) ---
# Note: S3_ENDPOINT should be hostname:port only (no http:// prefix)
# The S3_USE_SSL setting determines whether to use HTTPS or HTTP
S3_ENDPOINT=garage:3900
S3_REGION=garage
S3_BUCKET=doppler-photos
S3_USE_SSL=false
# Note: S3_ACCESS_KEY_ID and S3_SECRET_ACCESS_KEY are automatically injected
# by the garage-init service via /shared/s3-credentials.env